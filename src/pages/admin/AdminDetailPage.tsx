/** /admin/applications/:id: 申込詳細・審査入力。Issue #5 で実装予定。 */
export function AdminDetailPage() {
  return (
    <div className="min-h-screen bg-gray-100 py-8 flex items-center justify-center">
      <p className="text-gray-500 text-sm">申込詳細ページ（Issue #5 で実装）</p>
    </div>
  );
}
