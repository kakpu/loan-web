/** S99: 断り確定。Issue #3 で実装予定。 */
export function RejectedPage() {
  return (
    <div className="min-h-screen bg-gray-100 py-8 flex items-center justify-center">
      <p className="text-gray-500 text-sm">断り確定ページ（Issue #3 で実装）</p>
    </div>
  );
}
