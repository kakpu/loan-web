/** S04: 本人確認案内。Issue #3 で実装予定。 */
export function IdentityPage() {
  return (
    <div className="min-h-screen bg-gray-100 py-8 flex items-center justify-center">
      <p className="text-gray-500 text-sm">本人確認案内ページ（Issue #3 で実装）</p>
    </div>
  );
}
