/** S03: 即時与信結果。Issue #3 で実装予定。 */
export function CreditPage() {
  return (
    <div className="min-h-screen bg-gray-100 py-8 flex items-center justify-center">
      <p className="text-gray-500 text-sm">即時与信結果ページ（Issue #3 で実装）</p>
    </div>
  );
}
