/** S06: 契約案内。Issue #3 で実装予定。 */
export function ContractPage() {
  return (
    <div className="min-h-screen bg-gray-100 py-8 flex items-center justify-center">
      <p className="text-gray-500 text-sm">契約案内ページ（Issue #3 で実装）</p>
    </div>
  );
}
